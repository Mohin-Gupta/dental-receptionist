# Public values are embedded into the browser bundle at build time. They are
# configuration, never secrets. Rebuild the web image when changing them.
NEXT_PUBLIC_API_URL=https://api.example.com/api
NEXT_PUBLIC_BILLING_PLAN_KEYS=starter
